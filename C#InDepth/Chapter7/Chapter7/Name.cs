using System;

class Configuration { }
namespace Chapter7
{
    class Configuration { }

    class Test
    {
        /*
        static void Main()
        {
            Console.WriteLine(typeof(Configuration));
            //use global:: Configuration to tell the compiler exactly what you want
            Console.WriteLine(typeof(global::Configuration));
            Console.WriteLine(typeof(global::Chapter7.Test));

        }
        */       
    }
}
